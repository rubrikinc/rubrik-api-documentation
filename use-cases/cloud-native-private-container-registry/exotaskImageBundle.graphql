query ExotaskImageBundle {
  exotaskImageBundle {
  azureImages{
    bundleVersion
    repoUrl
    bundleImages
    {
      name
      tag
      sha
    }
  },
  awsImages{
    bundleVersion
    repoUrl
    bundleImages
    {
      name
      tag
      sha
    }
    eksVersion
  }
  }
}
